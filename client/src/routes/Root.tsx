import React from 'react';
import SideBar from '../components/SideBar';

function Root() {
    return (
        <div className="max-w-6xl mx-auto">
            <SideBar />
        </div>
    );
}

export default Root;
